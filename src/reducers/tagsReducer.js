import { GET_INTERESTS,
    GOT_INTERESTS,
    GETTING_TAGS_ERROR,
    GET_REQUIREMENTS,
    GOT_REQUIREMENTS,
    GET_CAUSE_AREAS,
    GOT_CAUSE_AREAS} from '../actions'

export const tagsReducer = (state, action) => {
    switch(action.type){
        case TEST:
            return {

            }
        default:
            return state
    }
}